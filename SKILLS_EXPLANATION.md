# شرح المهارات (Skills) المطبقة في مشروع NewElectroStore

هذا المستند يشرح تطبيق مبادئKarpathy Skills وأفضل الممارسات المعمارية في مشروع المتجر الإلكتروني الجديد.
كل مهارة لها جهة عمل واضحة ومسؤولية محددة، وفقًا لمبادئ الهندسة من CLAUDE.md.

## المهارات المطبقة:

### 1. مهارة البساطة (Simplicity Skill)
**الجهة العاملة:** جميع مكونات الواجهة والمنطق
**التطبيق:** 
- استخدام أبسط حل ممكن لتحقيق المتطلبات
- تجنب التعقيد الزائد في المكونات والخدمات
- مثال: بدلًا من إنشاءzustand معقد لإدارة الحالة، نستخدم React Context بسيط عندما يكون كافٍ
- في الواجهة: استخدام مكونات جاهزة من مكتبات UI موثوقة بدلاً من إعادة اختراع العجلة

### 2. مهارة عدم التكرار (DRY Skill)
**الجهة العاملة:** جميع الملفات والمكونات
**التطبيق:**
- استخراج المنطق المتكرر إلى هوكس مخصصة (custom hooks)
- إنشاء مكونات UI قابلة لإعادة الاستخدام (Buttons, Cards, Inputs)
- مشاركة الثوابت والتكوينات في ملفات مركزية
- مثال: إنشاء `useApi` هوك لاستدعاءات Supabase الموحدة

### 3. مهارة التغليف (Encapsulation Skill)
**الجهة العاملة:** المكونات والخدمات
**التطبيق:**
- إخفاء تفاصيل التنفيذ الداخلية للمكونات
- استخدام واجهات واضحة (props) للتواصل بين المكونات
- في الخدمات: إخفاء تفاصيل عميل Supabase خلف واجهة بسيطة
- مثال: إنشاء `SupabaseService` فئة encapsulate جميعOperations Supabase

### 4. مهارة التكوين على الوراثة (Composition over Inheritance)
**الجهة العاملة:** مكونات الواجهة
**التطبيق:**
- تفضيل تجميع المكونات الصغيرة لبناء مكونات complex
- تجنب hierarchies عميقة من المكونات الموروثة
- مثال: بناء ProductCard من ImageComponent + TextComponent + ButtonComponent بدلاً من فئة أساسية معقدة

### 5. مهارة الإعداد المспецифиكي للمزود (Provider-specific Config)
**الجهة العاملة:** إعدادات Supabase والخدمات الخارجية
**التطبيق:**
- الاحتفاظ بالإعدادات الخاصة بالمزود في بنّاء المزود فقط
- عدم تسريب إعدادات Supabase إلى منطق الأعمال العام
- مثال: إنشاء `SupabaseProvider` يدير initialize والتهيئة، بينما يستخدم باقي التطبيق واجهة بسيطة

### 6. مهارة إزالة الكود الميت (Dead Code Removal)
**الجهة العاملة:** الصيانة الدورية
**التطبيق:**
- إزالة أي كود غير مستخدم أو معلق
- إزالة التعليقات التوضيحية الزائدة التي لا تضيف قيمة
- مثال: بعد اختبار ميزة، إذا لم نعد نستخدم كودًا تجريبيًا، نزيله فورًا

### 7. مهارة التسمية محايدة للمنصة (Platform-agnostic Naming)
**الجهة العاملة:** جميع الثوابت وأسماء الأحداث
**التطبيق:**
- استخدام أسماء عامة مثل `REFRESH_DATA` بدلاً من `ANDROID_REFRESH_DATA`
- تجنب الأسماء المتعلقة بالمنصة في المنطق المشترك
- مثال: إنشاء eventiEvents 객체 مع أسماء أحداث محايدة تُستخدم في iOS وAndroid

### 8. مهارة запрет على تجاهل الأنواع (No Type Ignores)
**الجهة العاملة:** جميع الملفات TypeScript
**التطبيق:**
-禁止使用`// @ts-ignore` أو `// @ts-expect-error`
- إصلاح مشكلات الأنواع بدلاً من إخفائها
- استخدام الأنواع الصريحة والتحقق منها أثناء التطوير
- مثال: بدلاً من `any`، نحدد الأنواع الدقيقة للكائنات المرتجعة من Supabase

### 9. مهارة التكامل الكامل (Complete Migrations)
**الجهة العاملة:** إعادة الهيكلة والنقل
**التطبيق:**
- عند نقل ملف أو مكون، تحديث所有 الاستيرادات في نفس الالتزام
- عدم تركcling أو شيمات توافق إلا إذا كان مطلوبًا صراحةً لواجهة منشورة
- مثال: عند نقل مكون من `components/old` إلى `components/ui`, نقوم بتحديث所有 الاستيرادات في نفس التغيير

### 10. مهارة أقصى تغطية بالاختبارات (Maximum Test Coverage)
**الجهة العاملة:** الاختبار
**التطبيق:**
- كتابة اختبارات لجميع المنطق التجاري والخدمات
- предпочтение اختبار الدخان المباشر لاكتشاف الأخطاء مبكرًا
- في هذا المشروع: سنضع هيكل اختبارات حتى لو لم نكتب الاختبارات الفعلية الآن
- مثال: إنشاء مجلد `__tests__` مع هيكل جاهز للوحدات والاختبارات التكاملية

### 11. مهارة سير العمل المعرفي (Cognitive Workflow Skill)
**الجهة العاملة:** دورة التطوير
**التطبيق:**
1. **التحليل (ANALYZE):** قراءة الملفات ذات الصلة وعدم التخمين
2. **التخطيط (PLAN):** خرائط_logic وتحديد السبب الجذري أو التغييرات المطلوبة
3. **التنفيذ (EXECUTE):** إصلاح السبب وليس العرض، تنفيذ تدريجي مع ارتكازات واضحة
4. **التحقق (VERIFY):** تشغيل فحوصات ذات صلة وتأكيد الحل عبر السجلات أو المخرجات
5. **الخصوصية (SPECIFICITY):** فعل بالضبط ما هو مطلوب؛ لا أكثر ولا أقل
6. **الانتشار (PROPAGATION):** انتشار التغييرات بشكل صحيح في جميع الملفات المتأثرة
7. **الإصدار (VERSION):** إذا لامس الالتزام ملفات إنتاج على `main`، رفع semver في الالتزام نفسه

### 12. مهارة أدوات التطوير المفضلة (Preferred Tools Skill)
**الجهة العاملة:** جميع عمليات التطوير
**التطبيق:**
- تفضيل الأدوات المدمجة (مثل grep، node، npm) على سير العمل اليدوي
- التحقق من توفر الأداة قبل الاستخدام
- مثال: استخدام `grep -r` للبحث في الكود بدلاً من فتح الملفات يدويًا

## تطبيق هذه المهارات في هيكل المشروع:

### بنية المجلدات المقترحة:
```
/src
  /components     # مكونات UI قابلة لإعادة الاستخدام (مهارة DRY, Composition)
  /screens        # شاشات التطبيق (مهارة البساطة)
  /services       # خدمات الخارجية مثل Supabase (مهارة encapsulation, provider-specific config)
  /hooks          # هوكس مخصصة (مهارة DRY)
  /utils          # وظائف مساعدة عامة (مهارة DRY)
  /contexts       # سياقات React لإدارة الحالة العامة (مهارة البساطة عندما يكون كافيًا)
  /types          # تعريفات أنواع TypeScriptShared (مهارة لا تتجاهل الأنواع)
  /assets         # موارد ثابتة (صور، أيقونات)
  /themes         # إعدادات الثيمات والألوان
  /navigation     # إعدادات التوجيه
  /__tests__      # هيكل الاختبارات (مهارة أقصى تغطية)
```

### تطبيق المبادئ على ملفات التكوين:
1. **app.json/eas.json:** إعدادات بسيطة وواضحة (تبسيط)
2. **tsconfig.json:** إعدادات نوع صارمة بدون استثناءات (لا تتجاهل الأنواع)
3. **package.json:** تبعيات محددة ومحددةversion (إزالة كود ميت محتمل)
4. **.env.example:** نموذج متغيرات بيئة واضح (إعدادات مspecific)

## كيف ستساعدك هذه المهارات في بناء تطبيق بلا أخطاء:

1. **تقليل نقاط الفشل:** عبر البساطة وإزالة التعقيد غير الضروري
2. **سهولة الصيانة:** عبر DRY والتغليف والتكوين
3. **قلة الأخطاء:** عبر strict typing وفحص الأنواع المستمر
4. **قابلية التوسع:** عبر التكوين والهيكل الواضح
5. **وضوح الفريق:** عبر التسميات المحايدة والمسؤوليات المحددة

هذه المهارات لا تعني كتابة كود أكثر؛ تعني كتابة كود أفضل، أقل عرضة للأخطاء، وأسهل في الفهم والصيانة.

لتطبيق هذه المهارات في التنفيذ الفعلي، سنبدأ بإنشاء الهيكل الأساسي وتطبيقها تدريجيًا مع كل مكون نضيفه.