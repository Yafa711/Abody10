# كيف تطبق المهارات في سلوك التطوير اليومي

هذا المستند يشرح كيف تطبق كل مهارة ليس فقط في هيكل المشروع، بل في سلوكك اليومي أثناء التطوير.

## تطبيق المهارات في سير العمل

### 1. مهارة البساطة (Simplicity Skill) - في السلوك
**قبل كتابة أي سطر من الكود، اسأل نفسك:**
- ما هو أبسط حل يمكن أن يحل هذه المشكلة؟
- هل يمكنني تحقيق المتطلبات الحالية بأقل قدر ممكن من الكود؟
- هل هذا التعقيد يضيف قيمة فعلية أم أنه تعقيد مبكر؟

**مثال عملي:**
بدلاً من إنشاءzustand معقد لإدارة حالة السلة من البداية، ابدأ بـ React Context بسيط. فقط إذا أثبت الاستخدام أن السياق غير كافٍ، انتقل لحل أكثر تطوراً.

### 2. مهارة عدم التكرار (DRY Skill) - في السلوك
**قبل كتابة أي وظيفة أو مكون، ابحث أولاً:**
- هل هناك cutom hook أو utility function مشابه موجود؟
- هل يمكنني استخراج هذا المنطق إلى مكان مشترك بدلاً من نسخه؟

**مثال عملي:**
إذا وجدت نفسك تكتب نفس منطق fetch في ثلاثة مكونات مختلفة، استخرجه إلى `useApi` هوك واستخدمه في جميع المكونات.

### 3. مهارة التغليف (Encapsulation Skill) - في السلوك
**عند إنشاء أي وحدة (مكون، خدمة، هوك)، اسأل:**
- ما هي التفاصيل الداخلية التي يجب إخفاؤها عن الوحدات الأخرى؟
- ما هو الحد الأدنى من الواجهة التي أحتاجها لتفاعل الوحدات الأخرى مع هذه الوحدة؟
- كيف يمكنني منع الوحدات الأخرى من الاعتماد على تفاصيل تنفيذية قد تتغير؟

**مثال عملي:**
في خدمة Supabase، لا تُرجع كائن العميل الخام للمكونات. بدلاً من ذلك، قدم metodات واضحة مثل `getProducts()`, `createOrder()` التي تُخفي تفاصيل تنفيذ Supabase.

### 4. مهارة التكوين على الوراثة - في السلوك
**عند بناء أي واجهة معقدة، فكّر:**
- كيف يمكنني بناء هذا من مكونات أصغر وأنبسط؟
- ما هي أصغر أجزاء وظيفية يمكنني تجميعها لبناء=this المعقد؟

**مثال عملي:**
بدلاً من إنشاء `ProductCardWithActionsAndReviews` كمكون واحد ضخم، ابحث عن كيفية تجميع:
- `ProductImage` (للصورة)
- `ProductTitleAndPrice` (للعنوان والسعر)
- `ProductActions` (للإجراءات مثل إضافة للسلة)
- `ProductReviews` (للتعليقات)
ثم اجمع هذه المكونات الصغيرة لبناء البطاقة المعقدة.

### 5. مهارة الإعداد المспецифиكي للمزود - في السلوك
**عند العمل مع أي خدمة خارجية، تأكد من:**
- أن جميع إعدادات الخدمة محفوظة في مكان واحد ومحدد (مثل مجلد الخدمات)
- أن منطق الأعمال العام لا يعرف شيئًا عن تفاصيل إعداد هذه الخدمة
- أن تغيير المزود (مثل الانتقال من Supabase إلى Firebase) يتطلب تغييرات فقط في مكان الخدمة، وليس في كل مكان في الكود

**مثال عملي:**
بدلاً من استدعاء `supabase.from('users').update()` مباشرة في مكون الملف الشخصي، استدعِ `profileService.updateProfile(userData)` التي تخفي-deetails تنفيذ Supabase.

### 6. مهارة إزالة الكود الميت - في السلوك
**اجعل هذه습ة:**
- بعد كل feature branch أو اختبار لميزة جديدة، اسأل: "ما الكود التجريبي أو المعلق الذي يمكنني إزالته الآن؟"
- قبل الالتزام، نفّذ بحثًا سريعًا عن التعليقات التي توضح أنها مؤقتة أو تجريبية (`TODO: temporary`, `FIXME: remove after test`)
- راجعdependencies دورياً لإزالة تلك غير المستخدمة

### 7. مهارة التسمية محايدة للمنصة - في السلوك
**عند naming constants, events, أو حتى المتغيرات، اسأل:**
- هل هذا الاسم سيظل له نفس المعنى إذا انتقلنا إلى iOS أو إلى تطبيق ويب؟
- هل يحتوي الاسم على إشارات إلى منصة محددة قد لا تكون ذات صلة بالمنطق؟

**مثال عملي:**
بدلاً名前变量 `ANDROID_REFRESH_TRIGGER_COUNT`، استخدم `UI_REFRESH_TRIGGER_COUNT` أو أفضل jeszcze `DEFAULT_REFRESH_THRESHOLD` التي تصف ما يفعله العدد، وليس أين يُستخدم.

### 8. مهارة запрет на Ignoring类型的 - в поведении
**اجعل هذه القاعدة صارمة:**
- nunca usar `// @ts-ignore` ou `// @ts-expect-error`
- Se o TypeScript estiver reclamando, corrija o problema de tipo em vez de ignorá-lo
- Use tipos explícitos e preciso sempre que possível

**Пример prácticamente:**
Вместо того чтобы писать `const user = any;` и получать объект пользователя из Supabase, определите точный тип:
```typescript
type User = {
  id: string;
  email: string;
  role: 'customer' | 'admin';
  created_at: string;
};
const user: User = await supabaseService.getUserProfile();
```

### 9. مهارة التكامل الكامل - в поведении
**عند إجراء أي تغيير يؤثر على الاستيرادات، اتبع هذا الإجراء:**
1. قم بالتغيير (نقل ملف، إعادة تسمية، إلخ)
2. فورًا، قم بتحديث所有 الاستيرادات التي تأثرت بهذا التغيير
3. لا تترك questo para depois; gör det i samma commit

**مثال عملي:**
إذا przenosisz komponent `Button` ze `src/components/old/Button.jsx` do `src/components/ui/Button.jsx`, natychmiastowo znajdź i zaktualizuj każdy plik, który importował stary ścieżkę.

### 10. مهارة أقصى تغطية بالاختبارات - في السلوك
**اجعل الاختبار جزءًا من تعريف "الجاهز" لكل ميزة:**
- قبل اعتبار المهمة مكتملة، اسأل: "ما هو الحد الأدنى من الاختبارات التي أحتاجها لأكون واثقًا أن هذا يعمل?"
- حتى لو لم تكتب الاختبارات الآن، اترك مكانًا واضحًا لها (مثل إنشاء ملف `.test.js` vacío junto al archivo de implementación)

**مثال práctico:**
بعد إنشاء बहुत srpna `useAuth` 跳线, 创建一个 `__tests__/hooks/useAuth.test.js` 文件，即使它目前是空的。这为未来添加测试留下了空间。

### 11. مهارة سير العمل المعرفي - في السلوك
**اتبع هذه الخطوات بوعي لكل مهمة:**

1. **التحليل (ANALYZE):** 
   - اقرأ الملفات ذات الصلة قبل كتابة سطر واحد من الكود
   - لا تفترض؛ تحقق من how things actually work
   - مثال: قبل تعديل خدمة المصادقة، اقرأ `services/supabase.js` و`contexts/AuthContext.js` والمكونات التي تستخدمها

2. **التخطيط (PLAN):** 
   - ضع خرائط منطقية بسيطة للتغيير المطلوب
   - حدد السبب الجذري إذا كان إصلاح bug
   - اذكر بوضوح ما التغييرات المطلوبة ولماذا

3. **التنفيذ (EXECUTE):** 
   - أصلح السبب، وليس العرض
   - نفّذ تدريجيًا مع وجود ارتكازات واضحة (commits مع رسائل ذات معنى)
   - مثال: عند إصلاح bug في المصادقة، أولاً أصلح خدمة Supabase، ثم السياق، ثم المكونات التي تستخدمها

4. **التحقق (VERIFY):** 
   - شغّل فحوصات ذات صلة (اختبارات، linting، نوع فحص)
   - أكد الحل عبر السجلات أو المخرجات أو الاختبارات اليدوية البسيطة
   - مثال: بعد تغيير في خدمة Supabase، تأكد من أن المكونات التي تستقبل البيانات ما زالت تظهرها بشكل صحيح

5. **الخصوصية (SPECIFICITY):** 
   - افعل بالضبط ما هو المطلوب؛ لا أكثر ولا أقل
   - لا تضيف "مكتسبات" أو تحسينات لم تُطلب

6. **الانتشار (PROPAGATION):** 
   - تأكد من أن التغييرات انتشرت بشكل صحيح في جميع الملفات المتأثرة
   - إذا غيرت واجهة خدمة، تأكد من تحديث جميع المستهلِكين

7. **الإصدار (VERSION):** 
   - إذا لامس الالتزام ملفات إنتاج، قم بزيادة semver في الالتزام نفسه
   - لا تؤجل زيادة الإصدار إلى التزام مستقبلي

### 12. مهارة أدوات التطوير المفضلة - في السلوك
**استخدم الأدوات لتوفير الوقت وتقليل الأخطاء:**
- قبل البحث يدويًا في ملف، جرّب `grep -r "pattern" src/`
- قبل تشغيل تطبيق لاختبار تغيير بسيط، جرّب الاختبارات unit إذا وجدت
- قبل إضافة تبعية، تحقق مما إذا كان يمكنك تحقيق الهدف بالأدوات الموجودة

**أمثلة عملية:**
- instead من opening each file to find where a variable is used: `grep -r "userRole" src/`
- instead من manually checking if all ts files compile: `npm run type-check` (skriv denne script i package.json)
- instead من نسخ ولصق عقدة similaire: ابحث أولًا عما إذا كان هناك هوك أو وظيفة مساعدة موجودة

## تطبيق هذه المبادئ في مراجعات الكود الذاتية

قبل أن ترى أن المهمة مكتملة وتنتقل للمهمة التالية، اسأل نفسك هذه الأسئلة:

1. **البساطة:** هل هذا هو أبسط حل ممكن لتحقيق ما هو مطلوب؟
2. **DRY:** هل استخلصت كل المنطق المتكرر إلى هوكس، وظائف، أو مكونات مشتركة؟
3. **التغليف:** هل أخفيت تفاصيل التنفيذ المناسبة خلف واجهات واضحة؟
4. **التركيب:** هل بنيت المعقد من البسيط بدلًا من وراثة hierarchies معقدة؟
5. **الإعداد المspecific:** هل حفظت إعدادات المزود في مكانها المناسب دون تسريب؟
6. **الكود الميت:** هل أزلت كل الكود غير المستخدم، التعليقات التجريبية، والاختبارات المؤقتة؟
7. **التسمية المحايدة:** هل استخدمت أسماء لا تعتمد على المنصة في المنطق المشترك؟
8. **عدم تجاهل الأنواع:** هل أصلحت كل مشكلات الأنواع بدلًا من إخفائها بالتعليقات؟
9. **التكامل الكامل:** إذا غيرت موقع ملف أو اسمه، هل قمت بتحديث所有 الاستيرادات في نفس التغيير؟
10. **الاختبار:** هل تركت بنية جاهزة للاختبار حتى لو لم تكتب الاختبارات الآن؟
11. **سير العمل:** هل suivيت مراحل ANALYZE → PLAN → EXECUTE → VERIFY → SPECIFICITY → PROPAGATION → VERSION؟
12. **الأدوات المفضلة:** هل استخدمت أدوات التطوير المدمجة لتجنب العمل اليدوي المتكرر؟

إذا كان الجواب على أي سؤال "لا"، ارجع وعدل قبل المتابعة.

## تذكير أخير

هذه المهارات ليست عن كتابة كود أكثر. إنها عن كتابة:
- **أقل كودًا** من خلال البساطة وإزالة التكرار
- **أقل أخطاء** من خلال التغليف، الأنواع الصريحة، والاختبار
- **أسهل في الصيانة** من خلال التسمية الواضحة، والفصل الواضح للمسؤوليات
- **أسرع في التطوير** من خلال استخدام الأدوات المناسبة وتجنب العمل اليدوي المتكرر
- **أكثر مرونة للتغيير** من خلال التغليف والفصل الواضح للاهتمامات

ابدأ صغيرًا، طبق مهارة أو مهارتين بوعي في مهمتك التالية، وبعد أن تصبح عادة، أضف المزيد. التكرار الواعي هو المفتاح لتحويل هذه المبادئ من نظرية إلى ممارسة طبيعية.